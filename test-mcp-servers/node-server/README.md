# mcp-typescript-starter

```shell
npm install
npm run build
```

Test in agent:
* Get all the users
* Say Hello to Casey

Run transports:

```shell
npm run start:stdio
npm run start:stream-http
npm run start:sse
```

HTTP transports use `HOST` and `PORT`.
