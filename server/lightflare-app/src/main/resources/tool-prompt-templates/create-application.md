Create a new application.

Required:
- `name`

Optional:
- `description`
- `source_chat_session_id`
- `published_version_id`
- `triggers`

Note:
- A new application automatically creates initial version `1` in `draft` state.
